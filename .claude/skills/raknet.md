---
name: raknet
description: Detailed information about the RakNet project.
---

# RakNet

Face of Mankind uses RakNet 3.611 for communication.

- `msbuild` works on Windows environments via `cmake`.
- `GCC 4.8` is _strictly_ required for other hosts. Must use `just build` or `just build-cpp` or it will not work.
