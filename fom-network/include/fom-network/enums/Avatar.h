#pragma once

#include <fom-network/InteropTypes.h>

namespace FOMNetwork {
namespace Enum {

enum AvatarSex : uint8_t {
  MALE = 0,
  FEMALE = 1,
};

enum AvatarRace : uint8_t {
  WHITE = 0,
  BLACK = 1,
};

}  // namespace Enum
}  // namespace FOMNetwork
